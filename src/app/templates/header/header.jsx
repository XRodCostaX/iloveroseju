import NavHeader from '../../components/navheader/navheader'

NavHeader

const Header = (props) => {
  return (
    <header>
      <nav>
        <NavHeader />
      </nav>
    </header>
  )
}

export default Header
